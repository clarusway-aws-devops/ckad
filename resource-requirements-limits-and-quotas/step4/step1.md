- Create a pod with the `nginx:latest` image named `mypod` under the `test` namespace.

- Define the `memory limit` of the pod as the half of `maximum memory` limit of a container under the `test` namespace.